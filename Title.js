function drawTitleScreen() {
  background(30, 144, 255);
  fill(255); textSize(48); textAlign(CENTER, CENTER);
  text("탑플 - 탐험가의 일기", width / 2, height / 2 - 50);
  textSize(24); text("클릭하여 시작", width / 2, height / 2 + 20);
}